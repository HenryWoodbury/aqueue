import { useState } from 'react'

import { useDownloadRostersQuery } from '../../services/download'

const Download = () => {

/*
  const { data, error, isLoading } = useDownloadRostersQuery()
  console.info('data', data)
  console.info('error', error)
  console.info('isLoading', isLoading)
*/

  return (
    <section>
      <article>
        <h2>Download Rosters</h2>
        <p></p>
      </article>
    </section>
  )
}

export { Download }